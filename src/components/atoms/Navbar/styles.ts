import theme from '../../../styles/theme';

export const outer_container = `${theme.color.bg.white} border-b ${theme.color.border.xlightgray}`;

export const container = `${theme.layout.container} flex items-center py-4 ${theme.color.text.primary} font-medium`;

export const logo = `${theme.color.text.brand} text-2xl font-semibold`;

// export const nav_container = `hidden sm:flex flex-1 justify-center items-center gap-12`;

export const cta_container = `flex flex-1 justify-end items-center gap-5 `;

export const nav_item = `text-sm`;
