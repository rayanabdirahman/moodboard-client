import theme from "../../../styles/theme";

export const container = `flex flex-col w-full mb-4`;

export const base = `${theme.color.bg.lightgray} rounded py-2 px-4`;

export const error = `border ${theme.color.border.error}`;

export const errorMessage = `${theme.font.size.sm} ${theme.font.color.error}`;
