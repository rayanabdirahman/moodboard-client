import theme from "../../../styles/theme";

export const base = `${theme.font.color.white} rounded w-full py-2 mb-4`;

export const disabled = `${theme.color.bg.disabled} ${theme.font.color.disabled} disabled:cursor-not-allowed`;

export const primary = `${theme.color.bg.brand} hover:${theme.color.bg.darkbrand}`;
